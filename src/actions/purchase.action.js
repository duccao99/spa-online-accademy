// import {
//   GET_ALL_COURSES_PURCHASED,
//   ADD_PURCHASED,
// } from "../actionTypes/purchase.type";

// const getPurchasedCourseIdList = (course_id_list) => ({
//   type: GET_ALL_COURSES_PURCHASED,
//   payload: course_id_list,
// });

// const addNewPurchasedCourseIdList = (course_id) => ({
//   type: ADD_PURCHASED,
//   payload: course_id,
// });

// export const handleSaveCartData = (cartData) => (dispatch) => {
//   dispatch(saveCart(cartData));
// };
